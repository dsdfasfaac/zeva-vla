"""Zeva in-context causal learning components.

Components are imported from their explicit submodules so memory-only tooling
does not eagerly require torchvision or the compiled Mamba extension.
"""
