# Template only. The evaluator must send reset scope "episode" for a new
# environment instance and "attempt" between retries in that same episode.
{
  "policy_name": "zeva_policy",
  "zeva_method": "cte_eap_pim",
  "handoff_root": "__REQUIRED_HANDOFF_ROOT__",
  "foundation_checkpoint": "__REQUIRED_TRAINING_FOUNDATION_CHECKPOINT__",
  "zte_checkpoint": "__REQUIRED_FROZEN_CTE__",
  "cte_artifacts": "__REQUIRED_CTE_MEMORY_AND_LIVE_CACHE__",
  "retrieval_checkpoint": "__REQUIRED_FROZEN_TASK_LANGUAGE_CLASSIFIER__",
  "stage2_checkpoint": "__REQUIRED_PIM_STEP2000_CHECKPOINT__",
  "exploratory_epoch40": true,
  "baseline_only": false,
  "device": "cuda",
  "model_rng_seed": 20260919
}
